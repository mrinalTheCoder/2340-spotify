<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Spotify_Wrapped" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Spotify_Wrapped" parent="Base.Theme.Spotify_Wrapped" />

    <style name="LlmOutputText" parent="TextAppearance.AppCompat">
        <item name="android:textSize">16sp</item>
        <item name="android:background">@color/llmOutputBackground</item>
        <item name="android:textColor">@color/llmOutputText</item>
        <item name="android:padding">16dp</item>
    </style>

    <color name="llmOutputText">#FFFFFF</color>
    <color name="llmOutputBackground">#000000</color>
</resources>